/**
* Configuration
*
* All packages must export this for them to be considered valid packages.
*/
export { rocPackageConfig } from 'roc-package-core';
export { rocPackageMetaConfig } from 'roc-package-core';
